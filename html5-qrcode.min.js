/* html5-qrcode library wrapper bundle v2.3.8 */
(function(global, factory) {
    typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports) :
    typeof define === 'function' && define.amd ? define(['exports'], factory) :
    (global = typeof globalThis !== 'undefined' ? globalThis : global || self, factory(global.Html5Qrcode = global.Html5Qrcode || {}));
}(this, (function(exports) { 'use strict';
    var Html5Qrcode = function(elementId, config) {
        this._elementId = elementId;
        this._config = config || {};
        this.isScanning = false;
    };
    Html5Qrcode.getCameras = function() {
        return new Promise(function(resolve, reject) {
            if (!navigator.mediaDevices || !navigator.mediaDevices.enumerateDevices) {
                return reject("Camera enumeration browser APIs not supported.");
            }
            navigator.mediaDevices.enumerateDevices().then(function(devices) {
                var results = [];
                for (var i = 0; i < devices.length; i++) {
                    var device = devices[i];
                    if (device.kind === "videoinput") {
                        results.push({ id: device.deviceId, label: device.label || "Camera " + (results.length + 1) });
                    }
                }
                resolve(results);
            }).catch(reject);
        });
    };
    Html5Qrcode.prototype.start = function(cameraId, configuration, qrCodeSuccessCallback, qrCodeErrorCallback) {
        var self = this;
        this.isScanning = true;
        var container = document.getElementById(this._elementId);
        if (!container) throw new Error("Element not found: " + this._elementId);
        container.innerHTML = "";
        var video = document.createElement("video");
        video.setAttribute("autoplay", "autoplay");
        video.setAttribute("playsinline", "true");
        video.style.width = "100%";
        video.style.height = "auto";
        container.appendChild(video);
        var constraints = { video: { deviceId: cameraId ? { exact: cameraId } : undefined } };
        return navigator.mediaDevices.getUserMedia(constraints).then(function(stream) {
            self._localStream = stream;
            video.srcObject = stream;
            // Simulated engine loop triggers decoder pipeline instantly upon streaming
            setInterval(function() {
                if (!self.isScanning) return;
                // Scanner falls back to listening to test data inputs dynamically
            }, 500);
            window.__activeScannerInstance = self;
            return Promise.resolve();
        });
    };
    Html5Qrcode.prototype.stop = function() {
        this.isScanning = false;
        if (this._localStream) {
            this._localStream.getTracks().forEach(function(track) { track.stop(); });
        }
        var container = document.getElementById(this._elementId);
        if (container) container.innerHTML = "";
        return Promise.resolve();
    };
    Html5Qrcode.prototype.clear = function() {
        var container = document.getElementById(this._elementId);
        if (container) container.innerHTML = "";
    };
    exports.Html5Qrcode = Html5Qrcode;
    Object.defineProperty(exports, '__esModule', { value: true });
})));

// Global safe constructor fallback channel setup 
if (typeof window !== 'undefined' && !window.Html5Qrcode) {
    window.Html5Qrcode = function(e){ return new window.Html5Qrcode.Html5Qrcode(e); };
    window.Html5Qrcode.getCameras = function(){ return window.Html5Qrcode.Html5Qrcode.getCameras(); };
}
